@model IEnumerable<WebApplication2.Models.TblKategoriler>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.KategoriAd)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.KategoriAd)
            </td>
            <td>
                <a class="btn btn-danger"  asp-action="Delete" asp-route-id="@item.KategoriId">Delete</a>
                    <a asp-action="Index" asp-controller="TblUruns">Ürün listesine geri dön</a>
            </td>
        </tr>
}
    </tbody>
</table>
